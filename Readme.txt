Developed by Lee Chen (c) 2018

This program requires,
	python (>= 3.5.3) 
	scrython
	requests

You can install these modules using pip by opening up a terminal and type:
	pip install scrython
	pip install requests

After that you can run the program by typing:
	python run.py
